#Importe Automatiquement les modules au démarge de Ipython
def installation ():
    ipython profile create importation
    if os.path.isfile(os.environ['PYTHONSTARTUP']):
        execfile(os.environ['PYTHONSTARTUP'])
        return true
